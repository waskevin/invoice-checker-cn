PyMuPDF>=1.24,<2
# QtCore/QtGui/QtWidgets.  This is the PySide6 runtime without optional
# Qt add-ons, which keeps Windows installations smaller and avoids long paths.
PySide6-Essentials>=6.7,<7
openpyxl>=3.1,<4
pytest>=8.0,<9
