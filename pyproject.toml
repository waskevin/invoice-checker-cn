[project]
name = "invoice-checker"
version = "0.1.0"
description = "Windows desktop tool for batch invoice validation and totals"
requires-python = ">=3.12,<3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

